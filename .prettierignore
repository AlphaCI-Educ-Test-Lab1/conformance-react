# Generated by AlphaCI, not yours to style.
#
# INSTRUCTIONS.md holds a markdown table prettier wants to column-align.
# The JSON files are emitted by JSON.stringify, which expands short arrays
# that prettier would keep on one line — tsconfig.json fails on exactly that.
# Reimplementing prettier here to satisfy a check we control would be the
# tail wagging the dog; declaring them generated is the honest line. Every
# file the student actually writes is still checked, including their own
# markdown and any config they add.
INSTRUCTIONS.md
package.json
tsconfig.json
.prettierrc

# Build output and reports.
dist/
coverage/
.next/
out/
junit.xml
