// Pending until the functions in this project exist. The panel calls them,
// so rendering it throws until you implement them -- which would read as a
// failure rather than as work still to do.
//
// Implement them, then replace these with real tests.

it.skip('adds the starting values', () => {});
it.skip('adds what the user types', () => {});
it.skip('asks for numbers when an input is not one', () => {});
